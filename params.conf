<IfModule mod_evasive24.c>
  DOSHashTableSize   3097
  DOSPageCount       2          # Triggers block after 2 page requests
  DOSSiteCount       30         # Lowered from default 50 to trigger faster blocking
  DOSPageInterval    1          # Time window for PageCount (seconds)
  DOSSiteInterval    1          # Time window for SiteCount (seconds)
  DOSBlockingPeriod  300        # Block duration extended from 10s to 300s (5 minutes)
</IfModule>
